{
  "name": "spec-kit-light-version-demo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint .",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:integration": "playwright test --project=chromium",
    "test:integration:headed": "playwright test --headed --project=chromium",
    "mutation": "powershell -ExecutionPolicy Bypass -File ./scripts/run-mutation.ps1",
    "mutation:stryker": "stryker run"
  },
  "devDependencies": {
    "@stryker-mutator/core": "^8.7.1",
    "@stryker-mutator/vitest-runner": "^8.7.1",
    "@playwright/test": "^1.55.0",
    "eslint": "^9.36.0",
    "globals": "^16.4.0",
    "vitest": "^3.2.4",
    "vite": "^8.0.10"
  }
}
